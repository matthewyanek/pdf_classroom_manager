# Use SQLite for development (no server needed)
DATABASE_URL=sqlite:///./pdf_manager.db

# Use this for PostgreSQL when ready
# DATABASE_URL=postgresql://postgres:postgres@localhost/pdf_manager

# Security settings (simple for development)
SECRET_KEY=development_key_for_testing
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Storage settings
STORAGE_BUCKET=pdf-classroom-manager